import { ViewTemplate } from '../../../@microsoft/fast-element/index.js';
/**
 * Creates a side navigation template with unique IDs
 * @param {string} idPrefix - Prefix for all generated IDs
 * @returns {ViewTemplate} FAST HTML template for the side navigation
 */
export declare function createSideNavTemplate(idPrefix?: string): ViewTemplate;
