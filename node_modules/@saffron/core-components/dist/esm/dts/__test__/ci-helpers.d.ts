import type { Page, Locator } from '@playwright/test';
/**
 * CI-specific test utilities for handling flaky behavior in GitHub Actions
 */
export interface RetryOptions {
    maxAttempts?: number;
    delayMs?: number;
    timeoutMs?: number;
}
/**
 * Enhanced wait for elements with CI-specific timeout handling
 */
export declare function waitForElementCI(element: Locator, options?: {
    timeout?: number;
    stable?: boolean;
}): Promise<void>;
/**
 * Retry function execution with exponential backoff for CI reliability
 */
export declare function retryOperation<T>(operation: () => Promise<T>, options?: RetryOptions): Promise<T>;
/**
 * Check if running in CI environment
 */
export declare function isCI(): boolean;
/**
 * Get browser-specific timeout multiplier for CI
 */
export declare function getTimeoutMultiplier(browserName?: string): number;
/**
 * Enhanced boundingBox check with retry logic for CI environments
 */
export declare function getBoundingBoxCI(element: Locator, options?: RetryOptions): Promise<{
    width: number;
    height: number;
    x: number;
    y: number;
} | null>;
/**
 * Enhanced focus management with CI-specific handling
 */
export declare function manageFocusCI(page: Page, element: Locator, options?: {
    strategy?: 'tab' | 'click' | 'focus';
    attempts?: number;
}): Promise<boolean>;
/**
 * Enhanced page close handling for CI environments
 */
export declare function safePageOperation<T>(page: Page, operation: () => Promise<T>, fallbackValue?: T): Promise<T>;
