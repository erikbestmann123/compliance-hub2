import { normalizeDates } from './normalizeDates.js';

function normalizeInterval(context, interval) {
  var [start, end] = normalizeDates(context, interval.start, interval.end);
  return {
    start,
    end
  };
}

export { normalizeInterval };
